{
  "name": "tgds-2020",
  "version": "0.0.1",
  "description": "TGDS 2020",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "dev": "PARCEL_MAX_CONCURRENT_CALLS=3 parcel serve *.html --port 1234",
    "build": "yarn clear && NODE_ENV=development parcel build index.html --no-source-maps",
    "serve": "yarn build && serve dist/",
    "clear": "rm -R dist/* && rm -R .cache/*",
    "update": "npm-check -u && rm package-lock.json ; yarn"
  },
  "dependencies": {
    "gsap": "^3.3.3"
  },
  "devDependencies": {
    "@babel/core": "^7.10.2",
    "@babel/plugin-transform-runtime": "^7.10.1",
    "@babel/preset-env": "^7.10.2",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.8"
  }
}
